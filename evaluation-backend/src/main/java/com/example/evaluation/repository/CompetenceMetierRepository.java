package com.example.evaluation.repository;

import com.example.evaluation.model.CompetenceMetier;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CompetenceMetierRepository extends JpaRepository<CompetenceMetier, Long> {
}
